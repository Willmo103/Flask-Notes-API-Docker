version: '3.8'

services:
  web:
    build: .
    container_name: note-server
    image: note-server:latest
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=sqlite:///notes.db
    volumes:
      - ./app:/app
      - db-data:/data

volumes:
  db-data:
